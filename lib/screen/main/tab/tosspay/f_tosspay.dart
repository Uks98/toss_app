import 'package:flutter/material.dart';

class TossPayFragment extends StatelessWidget {
  const TossPayFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
